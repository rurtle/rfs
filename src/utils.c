#include "utils.h"

// TODO: Add necessary includes (e.g., <stdio.h>, <stdlib.h>, <stdarg.h>)

// 1. Implementation of Logging Helpers
// TODO: Implement the logging logic.
//       Example: Write to stderr or a log file.
// void rfs_log(const char *format, ...) {
//      va_list args;
//     va_start(args, format);
//     vfprintf(stderr, format, args);
//     va_end(args);
// }

// 2. Implementation of Path Helpers
// TODO: Implement path parsing logic.
// void split_path(const char *path, char *directory, char *filename) {
//     // 2a. Find the last slash in the path
//     // 2b. Copy string before slash to directory
//     // 2c. Copy string after slash to filename
// }

// 3. Implementation of Bit Manipulation
// TODO: Implement bitmap logic if applicable.
// int set_bit(unsigned char *bitmap, int index) {
//     // 3a. Calculate byte index and bit offset
//     // 3b. Set the bit using bitwise OR
// }
